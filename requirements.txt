# =============================================================================
# Proyecto Minero 4.0 - Dependencias
# =============================================================================
# Instalar con: pip install -r requirements.txt

# Core Data Science
pandas>=2.0.0
numpy>=1.24.0

# Machine Learning
# ⚠️ REPRODUCIBILIDAD (2026-07-22): el R²=0.319 de GeoMet cobre es sensible a la
# versión de scikit-learn. Con sklearn 1.9.0 (Python 3.12) reproduce en 0.319
# (auditoría 0.3296, pipeline 0.3190). Con sklearn 1.7.2 (Python 3.10) el MISMO
# código da ~0.11. Causa: GradientBoosting cambió comportamiento entre 1.7 y 1.9,
# y con n=92 (dato chico) esa diferencia mueve el R² de 0.11 a 0.32. Por eso se
# fija un piso en 1.9.0: sin este pin, quien re-corra con otra versión NO obtiene
# el número documentado. Verificar con `python verify_geomet_number.py`.
scikit-learn>=1.9.0
optuna>=3.0.0
joblib>=1.3.0

# Visualización
matplotlib>=3.7.0
seaborn>=0.12.0
# SciencePlots: estilo de publicación (IEEE/Nature) para los gráficos de
# entrenamiento y el informe científico (core/scientific_report.py). Opcional
# en el sentido de que el pipeline no falla si falta — cae a seaborn-whitegrid.
SciencePlots>=2.1.0

# UI / Progress
rich>=13.0.0

# Configuración
python-dotenv>=1.0.0

# Dashboard HMI
# [FIX] Dependencias faltantes que causaban ModuleNotFoundError en deploy limpio
streamlit>=1.30.0
plotly>=5.18.0

# Generación de reportes PDF
# [FIX] fpdf2 no estaba declarado pero se importaba en core/report_generator.py
fpdf2>=2.7.0

# =============================================================================
# Dependencias opcionales para desarrollo
# =============================================================================
# Descomentar si necesitas herramientas de desarrollo

# Testing
# pytest>=7.0.0
# pytest-cov>=4.0.0

# Linting & Formatting
# black>=23.0.0
# ruff>=0.1.0
# mypy>=1.0.0

# Pre-commit hooks
# pre-commit>=3.0.0
